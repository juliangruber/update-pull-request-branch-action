name: Update Pull Request branch
author: juliangruber
description: 'Update a Pull Request from its base branch'
branding:
  icon: 'git-pull-request'
  color: purple
inputs:
  github-token:
    description: 'GitHub Token'
    required: true
  number:
    description: 'Pull Request number'
    required: false
  async:
    description: 'Run asynchronously or not'
    required: false
    default: false
runs:
  using: 'node12'
  main: 'dist/index.js'
